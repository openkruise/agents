# Paused Retention

This package provides stateless, policy-free paused-retention parsing and
calculation. It distinguishes absence, valid values, and invalid values without
choosing a caller default or mutating Kubernetes resources.
